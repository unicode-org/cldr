# Test data for grapheme locale validity, canonicalization, and name generation
# Format:
# Generally has the following structure, but the values depend on the file.
# @<parameter>=<parameter value>
# <input> ; <expected output>

